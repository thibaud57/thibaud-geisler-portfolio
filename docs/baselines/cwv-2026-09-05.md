---
title: "Baseline Core Web Vitals — 5 septembre 2026"
description: "Confirmation en production du correctif CLS livré en v1.6.1 : 0,288 → 0,012 sur /fr desktop, sur 5 pages et 2 locales. PSI mobile a 86, seul le LCP reste hors cible a 3,9 s. Relevé 5 pages × 2 locales × 2 formats et composition du bundle JS, le consentement c15t y pesant plus que React."
date: "2026-09-05"
keywords: ["performance", "core-web-vitals", "baseline", "cdp", "cls", "tbt", "bundle"]
scope: ["docs", "ops", "perf"]
---

# Baseline Core Web Vitals — 5 septembre 2026

> **Contexte** : mesure après déploiement de `v1.6.2` ([PR #126](https://github.com/thibaud57/thibaud-geisler-portfolio/pull/126)), qui apporte le prérendu des pages projet et la sortie de `motion` hors de `MotionItem`. Le correctif CLS de la coquille PPR, lui, est en production depuis `v1.6.1` ([PR #121](https://github.com/thibaud57/thibaud-geisler-portfolio/pull/121)) : ce relevé le reconfirme sur un périmètre élargi. Répond aux points « Reste à faire » de la [baseline du 4 septembre](cwv-2026-09-04.md). Données **Lab** uniquement.

## Mesures

Outil : Chromium piloté par CDP (Playwright), throttling appliqué et non simulé, cache vidé avant chaque page. Desktop 1350×940, CPU ×1, 10 Mb/s / 40 ms. Mobile 412×823 (DPR 2,625), CPU ×4, 1,6 Mb/s / 150 ms. Aucune ressource tierce dans les requêtes sur la production, donc pas d'injection JS de Kaspersky (cf. gotcha `CLAUDE.md`) — ce qui ne prouve pas son absence totale : il s'interpose aussi sur le TLS sans apparaître comme ressource, et gonfle alors le TTFB d'un facteur 3 à 5 (0,55-0,75 s actif contre 0,13-0,19 s désactivé, même jour, même page). Les mesures de cette baseline ont été prises avec Kaspersky actif, les timings absolus sont donc à lire avec cette réserve ; le TTFB de production, relevé après désactivation, est de **0,13 à 0,19 s** sur les quatre pages testées, dans la cible.

### Desktop

| Page | CLS | LCP | FCP | Poids |
|------|-----|-----|-----|-------|
| `/fr` | **0,012** | 0,31 s | 0,31 s | 656 Ko |
| `/fr/projets` | 0 | 0,24 s | 0,24 s | 787 Ko |
| `/fr/services` | 0 | 0,24 s | 0,24 s | 612 Ko |
| `/fr/contact` | 0 | 0,26 s | 0,26 s | 619 Ko |
| `/fr/projets/[slug]` | 0 | 0,25 s | 0,25 s | 649 Ko |
| `/en` | **0,009** | 0,24 s | 0,24 s | 654 Ko |
| `/en/projets` | 0 | 0,26 s | 0,26 s | 783 Ko |
| `/en/services` | 0 | 0,23 s | 0,23 s | 610 Ko |
| `/en/contact` | 0 | 0,24 s | 0,24 s | 616 Ko |
| `/en/projets/[slug]` | 0 | 0,25 s | 0,25 s | 647 Ko |

### Mobile

| Page | CLS | FCP | Poids |
|------|-----|-----|-------|
| `/fr` | **0,052** | 0,95 s | 604 Ko |
| `/fr/projets` | 0 | 0,98 s | 643 Ko |
| `/fr/services` | 0 | 0,90 s | 572 Ko |
| `/fr/contact` | 0 | 0,88 s | 574 Ko |
| `/fr/projets/[slug]` | 0 | 0,92 s | 581 Ko |
| `/en` | **0,044** | 0,96 s | 602 Ko |
| `/en/projets` | 0 | 1,00 s | 641 Ko |
| `/en/services` | 0 | 0,89 s | 572 Ko |
| `/en/contact` | 0 | 0,88 s | 573 Ko |
| `/en/projets/[slug]` | 0 | 0,92 s | 580 Ko |

> ⚠️ **Deux limites, à ne pas maquiller en résultats.** Le throttling réseau ne s'applique pas au document de navigation (TTFB ~45 ms pour 40 à 150 ms imposés) : les LCP et FCP sont optimistes, [PSI](https://pagespeed.web.dev/) reste la référence. Et l'entrée LCP semblait absente en émulation mobile : c'était **Kaspersky** qui la supprime ; une fois désactivé, elle apparaît via `PerformanceObserver` posé avant chargement (`buffered: true`), `getEntriesByType` seul ne suffisant pas.

### Élément LCP

`H1`, 36 280 px², **en mobile comme en desktop**. Le LCP est donc un **texte** : il dépend de l'arrivée du HTML, du CSS bloquant et de la police display, jamais des images ni du JS. D'où un TBT divisé par 7 depuis mai sans effet sur le LCP, et les 590 ms de requêtes bloquantes comme seul levier qui compte.

### PageSpeed Insights, `/fr` mobile

Chiffre opposable, deux runs consécutifs. Lighthouse 13.4.1, Moto G Power, 4G lente.

| Métrique | Run 1 | Run 2 | Cible |
|----------|-------|-------|-------|
| Score performance | 86 | 87 | — |
| FCP | 1,4 s | 1,4 s | — |
| LCP | **3,9 s** | **3,9 s** | < 2,5 s |
| TBT | 70 ms | 60 ms | < 200 ms |
| CLS | 0,051 | 0,051 | < 0,1 |
| Speed Index | 3,6 s | 2,1 s | — |

Autres catégories : accessibilité 96, bonnes pratiques 100, SEO 100, navigation agentique 2/3.

> ⚠️ **Le TBT relevé en CDP était faux d'un facteur 5** (295-444 ms contre 60-70 ms chez PSI) : l'observer `longtask` sous CPU ×4 compte sa propre instrumentation. Colonnes retirées des tableaux ci-dessus. Le CLS, lui, concorde (0,051 contre 0,052).

## Comparaison avec les baselines précédentes

| Métrique | 2026-05-13 | 2026-09-04 | 2026-09-05 | Delta |
|----------|-----------|-----------|-----------|-------|
| CLS `/fr` desktop | 0,287 | 0,288 | **0,012** | **-0,276** |
| CLS `/fr/projets` desktop | 0,276 | — | **0** | **-0,276** |
| CLS `/fr` mobile | 0,051 | — | 0,051 | ≈ stable |
| LCP `/fr` mobile | 4,1 s | — | **3,9 s** | -0,2 s |
| TBT `/fr` mobile | 460 ms | — | **60-70 ms** | **-390 ms** |
| Score perf `/fr` mobile | 70 | — | **86-87** | **+16** |

Le correctif de la coquille PPR tient en production sur les cinq pages et les deux locales, là où la confirmation du 4 septembre ne portait que sur `/fr`. Le CLS mobile, lui, n'a jamais été le problème et ne bouge pas.

## Composition du bundle JS

`/fr`, 416 Ko réseau sur 24 chunks, identiques sur les trois pages mesurées : c'est un socle commun, pas un coût par page.

| Chunk | Réseau | Brut | Contenu identifié |
|-------|--------|------|-------------------|
| `0ndu-aeq3eh-2` | 95 Ko | 292 Ko | c15t + Radix |
| `2ui81u87vl5z5` | 72 Ko | 229 Ko | react-dom |
| `0_1jtpaur6k-2` | 43 Ko | 158 Ko | non identifié |
| `0alxr85nxg6on` | 35 Ko | 109 Ko | `motion` (`whileInView`) |
| `40c4z_a9qq_m-` | 27 Ko | 85 Ko | c15t |
| `16j8wd3-sn1f9` | 23 Ko | 61 Ko | lucide |

**Le premier poste n'est pas React mais le gestionnaire de consentement**, ~122 Ko réseau répartis sur deux chunks, contre 72 Ko pour `react-dom`.

## Diagnostic

### LCP mobile, seul indicateur hors cible

3,9 s contre 2,5 s visés et 4,1 s en mai : quatre mois sans bouger, seul indicateur hors cible. PSI impute **590 ms aux requêtes bloquant le rendu**, devant 43 Kio de JS inutilisé et 14 Kio transpilé pour navigateurs anciens. Le FCP à 1,4 s situe le retard avant le premier pixel. Deux tâches longues sont relevées, mais le TBT à 60-70 ms dit qu'elles ne coûtent presque rien.

### 216 divs avant le `H1`

`BackgroundRippleEffect` ([background-ripple-effect.tsx](../../src/components/aceternity/background-ripple-effect.tsx)) émet 216 cellules, 57 Ko de HTML, soit 64 % de ce qui précède le titre. Le coût n'est pas réseau (brotli écrase des divs quasi identiques) mais processeur : autant de nœuds à parser, styler et disposer avant de peindre le LCP, chacun portant `will-change-transform`, donc autant de couches de composition à préparer.

### `motion` toujours livré sur l'accueil

`MotionItem` n'importe plus rien de `motion` ([motion-item.tsx](../../src/components/ui/motion-item.tsx)), mais la lib reste tirée par `hyper-text` (Hero) et `word-rotate` (CTA finale), plus `border-beam` et `number-ticker` ailleurs. La PR #124 a réduit le nombre d'éléments animés, pas les 35 Ko du bundle.

### Prérendu des pages projet

Vérifié en production sur `/fr/projets/webapp-gestion-sinistres` : `X-Nextjs-Prerender: 1` et `X-Nextjs-Cache: HIT`, avec `canonical`, les trois `hreflang` et le JSON-LD `CreativeWork` + `BreadcrumbList` présents dans le `<head>` pour un user-agent quelconque.

## Leviers restants

- **Les 590 ms de requêtes bloquant le rendu**, premier levier du LCP et le seul qui vise l'indicateur hors cible
- **Alléger `BackgroundRippleEffect`** : moins de cellules, montage après le premier rendu, ou fond CSS sans DOM
- **Sortir `motion` de l'accueil** : `hyper-text` et `word-rotate` sont deux effets de texte, remplaçables par du CSS comme l'a été `MotionItem`
- **43 Kio de JS inutilisé et 14 Kio d'`Ancien JavaScript`** : vérifier la cible de transpilation
- **Identifier le chunk `0_1jtpaur6k-2`** (43 Ko réseau), aucun marqueur de lib connu n'y répond

> Le consentement n'est **pas** un levier : c15t ne monte plus ses surfaces UI dès qu'un consentement est enregistré (mesuré : 416 Ko / 24 chunks pour un visiteur neuf contre 404 Ko / 21 chunks au retour). Les ~122 Ko restants sont le `ConsentManagerProvider` headless, requis par [CalendlyWidget](../../src/components/features/contact/CalendlyWidget.tsx) qui conditionne l'iframe à `has('marketing')`. En sortir supposerait de remplacer c15t, un chantier de conformité et non une optimisation.

## Reminder

| Date | Action |
|------|--------|
| Après chaque optimisation perf majeure | Remesurer et créer `cwv-YYYY-MM-DD.md` à côté |
| À la prochaine mesure | Passer par PageSpeed Insights : le pilotage CDP ne capte pas le LCP mobile et surestime le TBT d'un facteur 5 |
