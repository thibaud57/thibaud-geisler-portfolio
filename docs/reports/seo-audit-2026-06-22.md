# Audit SEO automatisé — Feature 5 — 2026-06-22

## Résumé

- **Feature déployée dans le code** : OUI (commit #34, release v0.7.0, codebase actuel v1.4.1)
- **Site accessible** : NON — 503 sur toutes les URLs (Connection refused côté Dokploy)
- **Sub-projects validés** : 0/6 (audit non réalisable — site hors ligne)
- **Core Web Vitals testés** : 0/8 (audit non réalisable — site hors ligne)
- **Cause d'arrêt** : Site entier en panne au moment de l'audit (2026-06-22T09:00 UTC)

---

## Phase 1 — Détection déploiement

```
curl -sI https://thibaud-geisler.com/llms.txt
→ HTTP/2 503 (Connection refused — Next.js server not responding)

curl -sI https://thibaud-geisler.com/
→ HTTP/2 503

curl -sI https://thibaud-geisler.com/fr/
→ HTTP/2 503
```

**Diagnostic** : Le site entier retourne 503. Ce n'est **pas** une feature manquante — la Feature 5 SEO est bien présente dans le codebase (voir ci-dessous) mais le service Dokploy sur le VPS IONOS ne répond pas. La réponse 503 provient d'un reverse proxy en amont, avec un corps `upstream connect error or disconnect/reset before headers. retried and the latest reset reason: remote connection failure`.

**État du code** (vérifié localement sur la branche `main`) :
- `src/app/llms.txt/route.ts` — route handler présent ✅
- `src/app/robots.ts` — présent ✅
- `src/app/sitemap.ts` — présent ✅
- `src/app/[locale]/(public)/opengraph-image.tsx` — présent ✅
- `src/app/[locale]/(public)/projets/[slug]/opengraph-image.tsx` — présent ✅
- `src/components/seo/json-ld.tsx` — présent ✅
- `src/lib/seo/json-ld.ts` — présent ✅
- `src/lib/seo.ts` + `seo.test.ts` — présent ✅
- `generateMetadata` avec `buildPageMetadata` sur `/a-propos`, `/services`, `/projets` — présent ✅

**Derniers tags git** : `v1.4.1` (actuel), SEO introduit à `v0.7.0` (commit #34 `feat(seo): référencement complet — metadata, OG, sitemap, robots, JSON-LD, llms.txt`).

---

## Phase 2 — Audit par sub-project

> Audit impossible — site hors ligne (503). Les vérifications ci-dessous sont basées sur **l'inspection du code source** uniquement, sans validation HTTP en production.

### 01 metadata-base : ⚠️ (code OK, prod non vérifiable)
- `src/lib/seo.ts` expose `buildPageMetadata()` ✅
- `generateMetadata` présente sur toutes les pages publiques (code inspecté) ✅
- Test `src/lib/seo.test.ts` présent ✅
- **Non validé** : balises `<title>`, `<meta description>`, `og:*`, `twitter:*`, `hreflang` en prod

### 02 og-images : ⚠️ (code OK, prod non vérifiable)
- `opengraph-image.tsx` présent à la racine `[locale]` et sur `projets/[slug]` ✅
- **Non validé** : endpoint `/fr/opengraph-image` → 200 + `Content-Type: image/png`

### 03 sitemap-dynamique : ⚠️ (code OK, prod non vérifiable)
- `src/app/sitemap.ts` avec `buildSitemapEntries` + projets dynamiques ✅
- **Non validé** : `/sitemap.xml` → XML valide avec `<xhtml:link hreflang>` et `<lastmod>`

### 04 robots-txt : ⚠️ (code OK, prod non vérifiable)
- `src/app/robots.ts` : `Allow: /`, `Disallow: /api/`, `Sitemap: ${siteUrl}/sitemap.xml` ✅
- **Non validé** : `/robots.txt` HTTP 200 en prod

### 05 json-ld-profile-breadcrumb : ⚠️ (code OK, prod non vérifiable)
- `src/components/seo/json-ld.tsx` + `src/lib/seo/json-ld.ts` présents ✅
- Utilisé sur `/a-propos` (import `JsonLd` dans `a-propos/page.tsx`) ✅
- **Non validé** : `@type: ProfilePage`, `mainEntity.@type: Person`, `knowsAbout` Wikidata, BreadcrumbList sur les autres pages

### 06 llms-txt : ⚠️ (code OK, prod non vérifiable)
- `src/app/llms.txt/route.ts` : H1 `# Thibaud Geisler`, blockquote, sections `## Pages` et `## Optional` ✅
- `Content-Type: text/plain; charset=utf-8` dans le handler ✅
- **Non validé** : endpoint live en prod

---

## Phase 3 — Core Web Vitals

> Audit impossible — site hors ligne. PageSpeed Insights ne peut pas crawler un site en 503.

| URL | LCP | INP | CLS | Verdict |
|---|---|---|---|---------|
| /fr | — | — | — | ⚠️ site hors ligne |
| /fr/services | — | — | — | ⚠️ site hors ligne |
| /fr/projets | — | — | — | ⚠️ site hors ligne |
| /fr/a-propos | — | — | — | ⚠️ site hors ligne |
| /en | — | — | — | ⚠️ site hors ligne |
| /en/services | — | — | — | ⚠️ site hors ligne |
| /en/projets | — | — | — | ⚠️ site hors ligne |
| /en/a-propos | — | — | — | ⚠️ site hors ligne |

---

## Phase 4 — Checklist humaine (non-automatisable)

- [ ] **PRIORITÉ 1** : Rétablir le service Dokploy sur le VPS IONOS — vérifier le conteneur Docker, les logs Dokploy, et le statut du service
- [ ] Search Console (https://search.google.com/search-console) : propriété vérifiée, sitemap soumis, impressions/clicks sur les pages publiques
- [ ] Perplexity (https://perplexity.ai) : recherche `Thibaud Geisler freelance IA` → portfolio cité ?
- [ ] ChatGPT search : idem
- [ ] Google Rich Results Test (https://search.google.com/test/rich-results) sur :
  - https://thibaud-geisler.com/fr/a-propos → `Profile page` éligible 0 erreur ?
  - https://thibaud-geisler.com/fr/projets/<slug> → `Breadcrumbs` éligible 0 erreur ?

---

## Phase 5 — Actions correctives suggérées

### URGENT — Site hors ligne

1. **Connexion SSH au VPS IONOS** et vérification du statut du conteneur :
   ```bash
   docker ps -a | grep thibaud
   docker logs <container_id> --tail 50
   ```
2. **Vérifier Dokploy** : dashboard admin → statut de l'application → redémarrer si nécessaire
3. **Vérifier les ressources VPS** : `df -h` (espace disque), `free -m` (RAM), `top` (CPU) — une saturation peut provoquer un 503

### Post-rétablissement — Relancer l'audit

Une fois le site de retour, relancer cet audit automatisé pour valider les 6 sub-projects et les Core Web Vitals en production. Toutes les vérifications de Phase 2 et Phase 3 sont en attente.

---

*Audit généré automatiquement le 2026-06-22 à 09:00 UTC. Site injoignable : audit reporté, code source inspecté à la place.*
